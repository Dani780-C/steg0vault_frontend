export interface Resource {
    id: number,
    name: string | null,
    type: string | null,
    description: string | null,
    algorithm: string | null,
    saved: boolean | null,
    imageBytes: string | null,
    createdAt: '',
    modifiedAt: ''
}
